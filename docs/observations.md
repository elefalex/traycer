# Observations

Passive log of noteworthy things encountered while working in this codebase.
Captured during routine tasks; triaged in dedicated cleanup passes.

**Verify before fixing.** Each row was flagged with partial context — confirm the
issue is real before acting on it. The `V` column tracks human verification.

**Lifecycle:** ☐ → ☑ for `V` (verified) and `R` (resolved). Strike resolved rows.
Append-only — never reorder or delete.

**Categories:** `todo` `dead` `bug` `smell` `stale` · **Severity:** `P0` `P1` `P2`

| Date | Cat | Sev | File:Line | Hash | V | R | Observation |
|------|-----|-----|-----------|------|---|---|-------------|
| 2026-08-14 | stale | P2 | docs/DEVELOPMENT.md:41 | `6afe6b01` | ☐ | ☐ | Docs reference an unsigned side-load flag `--allow-empty-pubkeys` that no longer exists; neither `set-deploy-target.cjs` accepts it. The trust root is now committed in `clients/traycer-cli/src/config.ts`; the surviving dogfood flag is `--allow-unpinned-host`, and the real unsigned side-load path is `traycer host install --from <dir>` (sha256-only, no minisign). |
| 2026-08-14 | bug | P0 | tools/capture-proxy/src/scrub.ts:10 | `d6fabb69` | ☐ | ☑ | ~~`scrubValue`'s array branch recurses with a hardcoded `keyName=""` instead of the parent key, so `{token: ["secret-jwt-1"]}` (a token whose value IS an array of strings) is never redacted — the array elements leak verbatim into scrubbed recordings. No test in `scrub.test.ts` exercises a `token` key whose direct value is an array; the "nested-in-arrays" test only nests token inside an object that happens to sit inside an array.~~ Fixed in `c3a123e3` (parent `keyName` threaded through array recursion) with three regression tests; leak payload verified redacted at every depth. |
| 2026-08-14 | bug | P1 | tools/capture-proxy/src/scrub.ts:5-7 | `d6fabb69` | ☐ | ☐ | The "is this key named token" check only runs inside the `typeof value === "string"` branch, so a non-string token value (`{token: 12345}` or `{token: {value: "jwt"}}`) is never redacted — it round-trips unchanged. Matches the brief's literal wording ("any string value under a key named token"), so spec-faithful, but worth a residual-risk note for a capture harness whose whole purpose is secret redaction. |
| 2026-08-14 | smell | P2 | tools/capture-proxy/src/scrub.ts:34-38 | `d6fabb69` | ☐ | ☐ | `scrubRecording` reads the whole input file into memory, computes the full scrubbed output string, and writes it in one `writeFile` call with no temp-file+rename. A malformed JSONL line throws mid-`.map()` with no line number in the error; a large recording is fully buffered twice (in + out). Not a leak (writes are all-or-nothing and post-scrub), but worth hardening before this feeds a CLI operators run on arbitrary captures. |
| 2026-08-14 | smell | P2 | Makefile:56 | `6afe6b01` | ☐ | ☐ | `bunx vitest` / `bun run test` silently run the test worker under real Node, not Bun (bunx defers to Node when the resolved bin's shebang says `#!/usr/bin/env node`, unless `--bun` is passed). Every existing package here happens to work under plain Node, so this has never surfaced — but any future package whose code touches real Bun APIs (`Bun.serve`, etc., as `tools/capture-proxy/src/proxy-server.ts` now does) will hit `ReferenceError: Bun is not defined` unless its own `test` script explicitly forces `bun --bun vitest run`. Nothing in `AGENTS.md`/`docs/DEVELOPMENT.md` documents this gotcha; `tools/capture-proxy/package.json`'s `test` script was patched locally as part of task 6, but the convention isn't centralized so the next Bun-native package will likely rediscover it the hard way. |
| 2026-08-14 | smell | P2 | tools/capture-proxy/src/scrub.ts:23 | `d6fabb69` | ☐ | ☐ | `scrubFrame` scrubs only `frame.payload`, while the committed-fixture guard (`src/__tests__/fixtures.test.ts`) asserts over the *whole* frame record. The asymmetry is deliberate on the guard's side but means a stray secret-bearing field outside `payload` would fail the guard with no way for the scrub CLI to fix it — the operator's only recourse would be hand-editing the recording. Currently theoretical: `RecordedFrame`'s closed schema (ts/connId/leg/direction/kind/method/schemaVersion/payload) has no top-level token field. Becomes real if that schema ever gains one. |
| 2026-08-14 | smell | P2 | tools/capture-proxy/src/secret-rule.ts:71-74 | `86f56d14` | ☐ | ☐ | `UNREDACTED_SECRET_JSON_PATTERN` is built with `[...SECRET_KEYS].join("\|")` and does not escape regex metacharacters. Harmless today (all three keys — `token`, `apikey`, `refreshtoken` — are plain identifiers), but a future key containing `.`, `+`, `?` etc. would silently corrupt the alternation and weaken the last secrets gate rather than failing loudly. One-line `escapeRegExp` when a fourth key is added. |
| 2026-08-14 | bug | P2 | tools/capture-proxy/src/fixture-files.ts | `c44cf596` | ☐ | ☐ | The recursive fixture scan uses dirent `isFile()`/`isDirectory()`, both of which are false for symlink dirents. Symlinked *directories* are deliberately not traversed (cycle safety), but a symlinked `.jsonl` *file* placed directly under `fixtures/` (e.g. `fixtures/latest.jsonl -> ../recordings/boot.jsonl`) is also silently skipped — so it would never be guarded for residual secrets. Low probability: nothing in the documented runbook creates symlinks, and `scrub-cli.ts --out` writes real files. |
| 2026-08-14 | smell | P2 | tools/capture-proxy/src/proxy-server.ts | `32938391` | ☐ | ☐ | The proxy records a client frame *before* calling `.send()` on the upstream socket, so `recorded` counts frames written to the recording, not frames the host actually received. When an upstream closes mid-burst, Bun's socket can still read `readyState === OPEN` while `.send()` silently no-ops — one instrumented run recorded 1604 frames while upstream received 6. The exit summary no longer claims delivery (the misleading `dropped` counter was removed and a `truncatedConnections` warning added), but the gap is by design and a recording can still over-represent what reached the host. |
| 2026-08-14 | smell | P2 | tools/capture-proxy/src/frame-classifier.ts:42 | `d36f8241` | ☐ | ☐ | Frames that fail `JSON.parse` are stored with the raw text as `payload` (a bare string). A secret inside that string is invisible to every gate: `scrubValue` only key-matches inside structures, the guard's walker only inspects values under secret-named keys, and the belt-and-braces regex cannot match JSON-escaped `\"token\":\"` inside a string literal. Low likelihood on the local leg (plain UTF-8 JSON text), but it is the one shape the whole record→scrub→guard pipeline cannot inspect. |

TODO ALL
